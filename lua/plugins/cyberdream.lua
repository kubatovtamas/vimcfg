return {
	"scottmckendry/cyberdream.nvim",
	lazy = false,
	priority = 1000,
	-- No config here: Themery owns when/which variant is applied via the
	-- `before` hooks in themery.lua (variant = "light" / "default").
}
